# v0.1 Plugin Verifier Deprecation Report

Date: 2026-05-04
Plugin version verified: `com.productofamerica.turboassistant:0.0.1`

## Summary

**Zero deprecated-API hits across all three pinned IDEs.** No internal-API, experimental-API, override-only-API, or non-extendable-API usages reported either.

## IDEs verified (pinned)

| Type | Build | Verdict | Report directory |
|------|-------|---------|------------------|
| `IntelliJPlatformType.IntellijIdeaUltimate` (`2025.1.4`) | `IU-251.27812.12` | Compatible | `build/reports/pluginVerifier/IU-251.27812.12/` |
| `IntelliJPlatformType.WebStorm` (`2025.2.6`) | `WS-252.28539.21` | Compatible | `build/reports/pluginVerifier/WS-252.28539.21/` |
| `IntelliJPlatformType.IntellijIdeaUltimate` (`2025.2.6.1`) | `IU-252.28539.33` | Compatible | `build/reports/pluginVerifier/IU-252.28539.33/` |

Total verifier wall time: 3 min 9 s. Plugin and dependencies downloaded: 201.56 MB (cached locally).

## Per-IDE report contents

For each IDE, the verifier produced three files under `plugins/com.productofamerica.turboassistant/0.0.1/`:

- `verification-verdict.txt`: contains exactly the line `Compatible`.
- `dependencies.txt`: dependency tree of the plugin against that IDE; informational only.
- `telemetry.txt`: timing data; informational only.

The verifier did **not** generate any of the following (their absence is the signal that there are no findings):

- `compatibility-problems.txt`
- `compatibility-warnings.txt`
- `deprecated-usages.txt`
- `experimental-api-usages.txt`
- `internal-api-usages.txt`
- `non-extendable-api-usages.txt`
- `override-only-usages.txt`

## Notes

The HTML report at `build/reports/pluginVerifier/<build>/report.html` for each IDE shows the dependency tree highlighted as `Compatible` (green marker class `pluginOk`); no `pluginHasProblems` or `warnings` rows are present.

A handful of `(failed) ... Unavailable` lines appear inside the dependency trees (e.g. `com.intellij.modules.kotlin.k1`, `intellij.platform.frontend.split`). These are optional, conditionally-loaded modules that the verifier did not resolve in the standalone test layout. They are dependencies of bundled plugins (DevKit, JSON, YAML), not of `turbo-assistant`. They do not affect the verdict, are not actionable from this plugin's side, and are not deprecation hits.

## Action required

None for the v0.1 publication path. Re-run the verifier after each new IDE EAP/release that lands inside the `since-build="251"` window; the v0.2 work will likely add `process.env`-related JS PSI usages that the verifier will check anew.
