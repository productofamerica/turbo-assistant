{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["<warning descr="Package 'nonexistent' is not defined in this workspace.">nonexistent</warning>#build"] }
  }
}
